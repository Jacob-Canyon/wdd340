<% if (title){ %>
    <h1><%= title %></h1>
    <%} else {
        res.redifect('/')
    } %>
    <% if (errors) { %>
        <ul class="notice">
            <% errors.array().forEach(error => { %>
                <li><%= error.msg %></li>
            <%  }) %>
        </ul>
    <%} %>
    <%- messages() %>

    <div class="formDiv">
        <form action="/inv/addClass" method="post">
            <label>Classification Name</label><br>
            <span>Name must be alphabetical characters only</span>
            <input type="text" id="classificationName" name="classification_name" required pattern="[A-Za-z]"><br>
            <input type="submit" value="Add Class">
        </form>

    </div>