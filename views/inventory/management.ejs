<% if (title){ %>
    <h1><%= title %></h1>
    <%} else {
        res.redifect('/')
    } %>
    <% if (errors) { %>
        <ul class="notice">
            <% errors.array().forEach(error => { %>
                <li><%= error.msg %></li>
            <%  }) %>
        </ul>
    <%} %>
    <%- messages() %>
    <div class="manageDiv">
        <a class="manageLink" href="/inv/addClass">Add Classification</a><br>
        <a class="manageLink" href="/inv/addVehicle">Add New Vehicle</a>
    </div>