<% if (title){ %>
    <h1><%= title %></h1>
    <%} else {
        res.redifect('/')
    } %>
    <% if (errors) { %>
        <ul class="notice">
            <% errors.array().forEach(error => { %>
                <li><%= error.msg %></li>
            <%  }) %>
        </ul>
    <%} %>
    <%- messages() %>
    <div class="manageDiv">
        <div><a class="manageLink" href="/inv/addClass">Add Classification</a></div><br>
        <div><a class="manageLink" href="/inv/addVehicle">Add New Vehicle</a></div>
    </div>